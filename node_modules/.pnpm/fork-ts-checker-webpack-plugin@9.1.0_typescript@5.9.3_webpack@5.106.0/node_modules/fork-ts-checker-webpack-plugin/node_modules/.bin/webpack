#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../webpack/bin/webpack.js" "$@"
else
  exec node  "$basedir/../../../webpack/bin/webpack.js" "$@"
fi
# cmd-shim-target=/Users/tranthithuyngan/Documents/Self-learning/NestJS/url-shortener/node_modules/.pnpm/fork-ts-checker-webpack-plugin@9.1.0_typescript@5.9.3_webpack@5.106.0/node_modules/webpack/bin/webpack.js

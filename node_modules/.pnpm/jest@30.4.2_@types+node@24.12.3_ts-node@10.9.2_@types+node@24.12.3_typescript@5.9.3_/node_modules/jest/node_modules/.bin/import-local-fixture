#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../import-local/fixtures/cli.js" "$@"
else
  exec node  "$basedir/../../../import-local/fixtures/cli.js" "$@"
fi
# cmd-shim-target=/Users/tranthithuyngan/Documents/Self-learning/NestJS/url-shortener/node_modules/.pnpm/jest@30.4.2_@types+node@24.12.3_ts-node@10.9.2_@types+node@24.12.3_typescript@5.9.3_/node_modules/import-local/fixtures/cli.js
